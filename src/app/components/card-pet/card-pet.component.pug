figure
  img(src="assets/img/placeholder-img.png", alt="Image de l'animal")
  figcaption
    h2 {{cat?.name}}
    p {{cat?.origin}}
    a([routerLink]="[ {outlets: { 'modal': ['cat', cat?.id] } } ]") details
